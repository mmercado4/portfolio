---
const normalVideo = '/video/miguel_coder.mp4';
const reverseVideo = '/video/miguel_coder_reverse.mp4';
---

<video id="dev-video" width="640" height="360" autoplay>
  <source src="/video/miguel_coder.mp4" type="video/mp4" />
  Tu navegador no soporta video HTML5.
</video>

<script>
  const video = document.querySelector('#dev-video');

  console.log('video', video);

  if (video) {
    video.addEventListener('ended', () => {
      console.log('fin');
    });
  }
</script>
